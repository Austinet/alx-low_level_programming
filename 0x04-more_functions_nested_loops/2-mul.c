#include "main.h"
/**
 * mul - multiplies two digits and return the result
 * @a : first digit
 * @b : second digit
 * Return: the result of multiplication
 */
int mul(int a, int b)
{
return (a * b);
}
